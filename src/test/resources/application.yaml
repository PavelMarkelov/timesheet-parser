spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 150MB
      max-request-size: 150MB
  profiles:
    active: test
server:
  port: 8081
  servlet:
    context-path: /api
    session:
      timeout: 120m